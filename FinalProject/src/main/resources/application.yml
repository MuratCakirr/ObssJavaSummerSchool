spring:
  datasource:
    url: jdbc:mysql://localhost:3306/onlinemarketplace
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        globality_quoted_identifiers: true
